scenario = ActivityScenario.launch(NewPatternChartActivity.class);
scenario.onActivity ( activity -> {
    Intent intent = activity.getIntent();

    intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);

    System.out.println(scenario.getResult().getResultCode());
});



scenario = ActivityScenario.launch(NewPatternChartActivity.class);
scenario.onActivity ( activity -> {
    Intent intent = activity.getIntent();

    intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
    PatternChartUtilKt.goEdit(context, type, feedLog);
    System.out.println(scenario.getResult().getResultCode());
});
///android.util.AndroidRuntimeException: Calling startActivity() from outside of an Activity  context requires the FLAG_ACTIVITY_NEW_TASK flag. Is this really what you want?




scenario = ActivityScenario.launch(NewPatternChartActivity.class);
scenario.onActivity ( activity -> {
            Intent intent = new Intent(context, FeedingLogActivity.class);
            intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
            
            activity.startActivity(intent);
}); // pass


scenario = ActivityScenario.launch(NewPatternChartActivity.class);
scenario.onActivity ( activity -> {
            Intent intent = new Intent(context, FeedingLogActivity.class);
            intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
            
            activity.startActivity(intent);
            PatternChartUtilKt.goEdit(context, type, feedLog);
});  //android.util.AndroidRuntimeException: Calling startActivity() from outside of an Activity  context requires the FLAG_ACTIVITY_NEW_TASK flag. Is this really what you want?



scenario = ActivityScenario.launch(NewPatternChartActivity.class);
scenario.onActivity ( activity -> {
            Intent intent = new Intent(context, FeedingLogActivity.class);
            intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);

            PatternChartUtilKt.goEdit(context, type, feedLog);
});     //android.util.AndroidRuntimeException: Calling startActivity() from outside of an Activity  context requires the FLAG_ACTIVITY_NEW_TASK flag. Is this really what you want?





        scenario = ActivityScenario.launch(FeedingLogActivity.class);
            scenario.onActivity ( activity -> {
            Intent intent = new Intent(context, FeedingLogActivity.class);
            intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);

            PatternChartUtilKt.goEdit(context, type, feedLog);
        }); //java.lang.NullPointerException: Cannot run onActivity since Activity has been destroyed already



        scenario = ActivityScenario.launch(FeedingLogActivity.class);
            scenario.onActivity ( activity -> {
            Intent intent = new Intent(context, FeedingLogActivity.class);
            intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);

        }); //java.lang.NullPointerException: Cannot run onActivity since Activity has been destroyed already




scenario = ActivityScenario.launch(NewPatternChartActivity.class);
            scenario.onActivity ( activity -> {
			Intent intent = activity.getIntent();
            intent.setClass(context, FeedingLogActivity.class);
            intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);

            PatternChartUtilKt.goEdit(context, type, feedLog);

        }); //android.util.AndroidRuntimeException: Calling startActivity() from outside of an Activity  context requires the FLAG_ACTIVITY_NEW_TASK flag. Is this really what you want?




ActivityScenario scenario = ActivityScenario.launch(NewPatternChartActivity.class);
PatternChartType type = PatternChartType.fromOrdinal(0);
BabyFeedData feedLog = new BabyFeedData();
feedLog.setFeedType(1);
feedLog.setBabyId(001);

scenario.onActivity ( activity -> {
	Intent intent = new Intent(context, FeedingLogActivity.class);
	
	intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);

	context.startActivity(intent);

	PatternChartUtilKt.goEdit(context, type, feedLog);

    System.out.println("=========");
});
      


      adb shell am instrument -w -m    -e debug false -e class 'com.glow.android.baby.ui.newpatternchart.PatternChartUtilTest#testGoEdit' com.glow.android.baby.debug.test/androidx.test.runner.AndroidJUnitRunner


#### General info
- Device: samsung S10
- Account: mandy1012@glowing.com (5866888)
- Environment: sandbox

#### Description

需要测一下打开所有页面（premium/non premium）, 如果有影响，只会有两种表现：
1，打开页面直接crash
2,   打开页面是空白





10:22
然后再测一下主要功能，所有的log都可以正常记
10:23
最后再测一下有很多用户数据的老版本，登录状态覆盖安装上来，没有问题（主要是记log, 看summary, chart)
tutorial也要测一下

